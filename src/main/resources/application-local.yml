server:
  port: 8080

  servlet:
    session:
      cookie:
        http-only: true
        secure: false
        same-site: lax

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.cloud.gateway: DEBUG